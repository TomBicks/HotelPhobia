scr_text("Here's a test to see what happens.");
scr_text("Here's a second test to see what happens if we have a much longer string that maybe goes on for a bit too long but oh well that's why we call this a test.");
scr_text("LAST ONE I PROMISE...");
scr_text("Ah why not; here's one more!");
